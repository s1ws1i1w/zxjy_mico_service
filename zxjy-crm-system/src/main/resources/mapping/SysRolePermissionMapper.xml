<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bs.union.crm.mapper.user.SysRolePermissionMapper">

  <!-- 实体映射 -->
  <resultMap id="BaseResultMap" type="com.bs.union.crm.model.entity.SysRolePermissionEntity">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="sys_role_id" jdbcType="BIGINT" property="sysRoleId" />
    <result column="sys_permission_id" jdbcType="BIGINT" property="sysPermissionId" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
  </resultMap>

  <!-- 表基本列 -->
  <sql id="Base_Column_List">
    id, sys_role_id, sys_permission_id, create_time
  </sql>

  <!-- 通用查询sql -->
  <sql id="Base_Where_SQL">
    <where>
		<if test="sysRoleId != null and !&quot;&quot;.equals(sysRoleId)">and sys_role_id = #{sysRoleId}</if>
		<if test="sysPermissionId != null and !&quot;&quot;.equals(sysPermissionId)">and sys_permission_id = #{sysPermissionId}</if>
		<if test="createTime != null and !&quot;&quot;.equals(createTime)">and DATE_FORMAT(create_time,'%Y-%m-%d') = STR_TO_DATE(#{createTime},'%Y-%m-%d')</if>
	</where>
  </sql>

  <!-- 插入一条记录 -->
  <insert id="save" keyProperty="id" useGeneratedKeys="true">
    insert into sys_role_permission
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="null != sysRoleId">sys_role_id, </if>
			<if test="null != sysPermissionId">sys_permission_id, </if>
			<if test="null != createTime">create_time, </if>
		</trim>
	values
		<trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="null != sysRoleId">#{sysRoleId}, </if>
			<if test="null != sysPermissionId">#{sysPermissionId}, </if>
			<if test="null != createTime">#{createTime}, </if>
		</trim>
  </insert>

  <!-- 更新一条记录 -->
  <update id="update">
    update sys_role_permission
		<set>
			<if test="null != sysRoleId">sys_role_id = #{sysRoleId}, </if>
			<if test="null != sysPermissionId">sys_permission_id = #{sysPermissionId}, </if>
			<if test="null != createTime">create_time = #{createTime}, </if>
		</set>
		where id = #{id}
  </update>

  <!-- 删除给定主键的记录 -->
  <delete id="delete">
    delete
		from sys_role_permission
	where
		id = #{id}
  </delete>

  <!-- 多个主键批量删除记录 -->
  <delete id="deleteBatch">
    delete
		from sys_role_permission
	where
		id in
		<foreach collection="array" index="index" item="item" open="(" separator="," close=")">
			#{item}
		</foreach>
  </delete>

  <!-- 多个实体批量删除记录 -->
  <delete id="deleteList">
    delete
		from sys_role_permission
	where
		id in
		<foreach collection="list" index="index" item="item" open="(" separator="," close=")">
			#{item.id}
		</foreach>
  </delete>

  <!-- 主键查找一条记录 -->
  <select id="findOne" resultMap="BaseResultMap">
    select
		<include refid="Base_Column_List" />
	from sys_role_permission
	where id = #{id}
  </select>

  <!-- 查询多条 -->
  <select id="findList" resultMap="BaseResultMap">
    select
		<include refid="Base_Column_List" />
	from sys_role_permission
		<include refid="Base_Where_SQL" />
  </select>

  <!-- 分页查询 -->
  <select id="findPage" resultMap="BaseResultMap">
    select
		<include refid="Base_Column_List" />
	from sys_role_permission
		<where>
		<if test="sysRoleId != null and !&quot;&quot;.equals(sysRoleId)">and sys_role_id = #{sysRoleId}</if>
		<if test="sysPermissionId != null and !&quot;&quot;.equals(sysPermissionId)">and sys_permission_id = #{sysPermissionId}</if>
		<if test="createTime != null and !&quot;&quot;.equals(createTime)">and DATE_FORMAT(create_time,'%Y-%m-%d') = STR_TO_DATE(#{createTime},'%Y-%m-%d')</if>
	</where>
  </select>

  <!-- 查询记录数量 -->
  <select id="count" resultType="int">
    select
		count(*)
	from sys_role_permission
		<include refid="Base_Where_SQL" />
  </select>

  <!-- 查询记录是否存在 -->
  <select id="exist" resultMap="BaseResultMap" />


</mapper>
